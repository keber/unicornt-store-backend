meta {
  name: Register
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/auth/register
  body: json
  auth: none
}

body:json {
  {
    "firstName": "Smoke",
    "lastName": "Test",
    "email": "{{email}}",
    "password": "{{password}}"
  }
}

assert {
  res.status: eq 201
}
