meta {
  name: Create Category (ADMIN)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/categories
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "name": "Accessories"
  }
}
